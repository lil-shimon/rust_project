fn main() {
    // state definition ( Board )

    // Player definition
    // x => you
    // o => computer
    pub enum Player {
        x,
        o,
    }

    // score definition

    // move func

    // check func  (is win, lose)
}
